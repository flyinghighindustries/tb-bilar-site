buildConfiguration:
  buildCommand: npm run build
  installDependenciesStep:
    command: npm install
    requiredFiles:
      - package.json
      - package-lock.json
livePreviewConfiguration:
  setupCommand: ":"
  serveCommand: npm run dev
